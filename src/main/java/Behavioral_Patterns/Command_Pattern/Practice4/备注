思路1 ： 将具体命令类保存至CommandStack中。【失败】
    问题：CommandStack栈中保存的是同一个具体命令类，后面再进行运算时，行参改变，栈中的型参也被改变

思路2 ： 将CommandStack中只存放接受者处理完的数据；撤销栈始终保存加法运算前的数据，
        1.当有一个插销操作后，将接受的撤销前的信息保存至恢复栈中
            1.1 如果撤销栈为空 ： return -1；
            1.2 如果恢复栈为空 ： 输出没有撤销操作，并打印当前接受者的信息

        2.当有一个恢复操作后，将恢复栈中弹出一个元素
            【恢复栈和方法待继续完善】

